async function support(){head('Support Tickets','Manage customer tickets, update status/progress, add internal notes and customer-visible updates.',''); const rows=await api('/api/internal/support/tickets'); $('#content').innerHTML=`<div class="panel">${table(['ID','Customer','Subject','Issue','Priority','Status','Progress','Assigned','Updated','Action'],rows.map(t=>[t.id,t.organization_id,esc(t.subject),esc(t.issue_type),badge(t.priority),badge(t.status),`${t.progress_percent}%`,esc(t.assigned_to||'-'),date(t.updated_at),`<button class="btn sm" onclick="ticketDetail(${t.id})">Manage</button>`]))}</div>`}
async function ticketDetail(id){const d=await api(`/api/internal/support/tickets/${id}`); const t=d.ticket; $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card"><div class="panel-head"><h2>Ticket #${t.id}: ${esc(t.subject)}</h2><button class="btn" onclick="closeModal()">Close</button></div><p>${esc(d.organization.name)} · ${badge(t.priority)} · ${badge(t.status)}</p><div class="progress"><span style="width:${t.progress_percent}%"></span></div><div class="split" style="margin-top:18px"><div class="panel"><h2>Update progress</h2><div class="form-grid"><label>Status<select id="ti_status"><option>open</option><option>in_progress</option><option>waiting_customer</option><option>resolved</option><option>closed</option></select></label><label>Priority<select id="ti_priority"><option>low</option><option>normal</option><option>high</option><option>urgent</option></select></label><label>Assigned to<input id="ti_assigned" value="${esc(t.assigned_to||user.email)}"></label><label>Progress %<input id="ti_progress" type="number" value="${t.progress_percent}"></label><label>Customer update<textarea id="ti_update">${esc(t.last_customer_update||'')}</textarea></label><label>Note<textarea id="ti_note">We are reviewing this ticket.</textarea></label><label>Visible to customer<select id="ti_visible"><option value="true">true</option><option value="false">false</option></select></label><button class="btn primary" onclick="saveTicket(${id})">Save Ticket</button></div></div><div class="panel"><h2>Conversation</h2>${table(['Author','Visible','Body','Time'],d.comments.map(c=>[esc(c.author_email),c.visible_to_customer?'yes':'no',esc(c.body),date(c.created_at)]))}<h2 style="margin-top:24px">Internal notes</h2>${table(['By','Visible','Note','Time'],d.internal_notes.map(n=>[esc(n.created_by),n.visible_to_customer?'yes':'no',esc(n.note),date(n.created_at)]))}</div></div></div>`; ti_status.value=t.status;ti_priority.value=t.priority;}
async function saveTicket(id){await api(`/api/internal/support/tickets/${id}`,{method:'PUT',body:JSON.stringify({status:ti_status.value,priority:ti_priority.value,assigned_to:ti_assigned.value,progress_percent:Number(ti_progress.value),last_customer_update:ti_update.value,note:ti_note.value,visible_to_customer:ti_visible.value==='true'})}); await ticketDetail(id)}
