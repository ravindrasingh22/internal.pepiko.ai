async function audit(){head('Internal Audit Logs','Every internal customer-impacting action is recorded with actor, reason, before/after and timestamp.',''); const rows=await api('/api/internal/audit-logs'); $('#content').innerHTML=`<div class="panel">${table(['Actor','Action','Customer','Resource','Reason','Time'],rows.map(a=>[esc(a.actor_email),esc(a.action),a.customer_org_id||'-',esc(a.resource_type),esc(a.reason||'-'),date(a.created_at)]))}</div>`}
