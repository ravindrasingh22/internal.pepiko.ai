async function reports(){head('Risk Reports','Aggregated safety and billing signals. Raw child payloads are not stored by default.',''); const d=await api('/api/internal/reports'); $('#content').innerHTML=`<div class="split"><div class="panel"><h2>Risk levels</h2>${table(['Risk','Count'],d.risk_levels.map(x=>[badge(x.risk_level),money(x.count)]))}<h2 style="margin-top:24px">Categories</h2>${table(['Category','Count'],d.categories.map(x=>[esc(x.category),money(x.count)]))}</div><div class="panel"><h2>Daily volume</h2>${table(['Date','Requests','Credits'],d.daily.map(x=>[esc(x.date),money(x.requests),money(x.credits)]))}</div></div>`}
